# JC Torres — portfolio

Single page static site. No build step, no dependencies.

## Deploy

Live on GitHub Pages from `main`.

To use Vercel instead:

```bash
vercel login
vercel --prod
```

## Edit

Everything is in `index.html`, content and styles together.
